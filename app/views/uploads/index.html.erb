<% content_for(:title_code, 'uploads.index.name') %>
<% content_for(:page_class, 'uploads') %>

<p><%= link_to translate('uploads.labels.new_upload'), new_upload_path() %></p>
<table id='uploadsTable'>
  <thead>
    <tr>
      <th><%= translate('app.labels.subject') %></th>
      <th><%= translate('app.labels.grade') %></th>
      <th><%= translate('app.labels.language') %></th>
      <th><%= translate('app.labels.status') %></th>
      <th><%= translate('app.labels.action') %></th>
    </tr>
  </thead>
  <tbody>
    <% @uploads.each do |u| %>
      <tr id="id_<%= u.id %>">
        <td><%= u.subject.code %></td>
        <td><%= u.grade_band.code %></td>
        <td><%= u.locale.name %></td>
        <td><%= BaseRec::UPLOAD_STATUS[u.status] %></td>
        <td>
          <%= link_to translate('uploads.labels.upload'), start_upload_upload_path(id: u.id) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
